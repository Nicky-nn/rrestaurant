// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { RESTPEDIDOFRAGMENT } from '../fragments/RestPedidoFragments'
import { EntidadParamsInput, RestPedido } from '../types'

export interface RestPedidoCancelarVariables {
  id: string
  entidad: EntidadParamsInput
}

export const RESTPEDIDOCANCELAR = gql`
  mutation RestPedidoCancelar($id: ID!, $entidad: EntidadParamsInput!) {
    restPedidoCancelar(id: $id, entidad: $entidad) {
      ...RestPedidoFragment
    }
  }
  ${RESTPEDIDOFRAGMENT}
`

export const useRestPedidoCancelar = (
  options?: UseMutationOptions<RestPedido, Error, RestPedidoCancelarVariables>,
) => {
  return useMutation<RestPedido, Error, RestPedidoCancelarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ restPedidoCancelar: RestPedido }>(RESTPEDIDOCANCELAR, variables)
      return response.restPedidoCancelar
    },
    ...options,
  })
}
