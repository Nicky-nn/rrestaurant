// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'

export interface RestEspacioEliminarVariables {
  id: string
}

export const RESTESPACIOELIMINAR = gql`
  mutation RestEspacioEliminar($id: ID!) {
    restEspacioEliminar(id: $id)
  }
`

export const useRestEspacioEliminar = (
  options?: UseMutationOptions<boolean, Error, RestEspacioEliminarVariables>,
) => {
  return useMutation<boolean, Error, RestEspacioEliminarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ restEspacioEliminar: boolean }>(RESTESPACIOELIMINAR, variables)
      return response.restEspacioEliminar
    },
    ...options,
  })
}
