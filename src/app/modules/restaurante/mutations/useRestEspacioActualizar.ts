// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { RESTESPACIOFRAGMENT } from '../fragments/RestEspacioFragments'
import { RestEspacio, RestEspacioActualizarInput } from '../types'

export interface RestEspacioActualizarVariables {
  id: string
  input: RestEspacioActualizarInput
}

export const RESTESPACIOACTUALIZAR = gql`
  mutation RestEspacioActualizar($id: ID!, $input: RestEspacioActualizarInput!) {
    restEspacioActualizar(id: $id, input: $input) {
      ...RestEspacioFragment
    }
  }
  ${RESTESPACIOFRAGMENT}
`

export const useRestEspacioActualizar = (
  options?: UseMutationOptions<RestEspacio, Error, RestEspacioActualizarVariables>,
) => {
  return useMutation<RestEspacio, Error, RestEspacioActualizarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ restEspacioActualizar: RestEspacio }>(
        RESTESPACIOACTUALIZAR,
        variables,
      )
      return response.restEspacioActualizar
    },
    ...options,
  })
}
