// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { SALIDAFACTURAFRAGMENT } from '../fragments/SalidaFacturaFragments'
import { EntidadParamsInput, SalidaFactura } from '../types'

export interface RestFacturaAnularVariables {
  entidad?: EntidadParamsInput
  notificacion?: boolean
  cuf: string
  codigoMotivo: number
}

export const RESTFACTURAANULAR = gql`
  mutation RestFacturaAnular(
    $entidad: EntidadParamsInput
    $notificacion: Boolean
    $cuf: String!
    $codigoMotivo: Int!
  ) {
    restFacturaAnular(
      entidad: $entidad
      notificacion: $notificacion
      cuf: $cuf
      codigoMotivo: $codigoMotivo
    ) {
      ...SalidaFacturaFragment
    }
  }
  ${SALIDAFACTURAFRAGMENT}
`

export const useRestFacturaAnular = (
  options?: UseMutationOptions<SalidaFactura, Error, RestFacturaAnularVariables>,
) => {
  return useMutation<SalidaFactura, Error, RestFacturaAnularVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ restFacturaAnular: SalidaFactura }>(
        RESTFACTURAANULAR,
        variables,
      )
      return response.restFacturaAnular
    },
    ...options,
  })
}
