// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments'
import { AperturaCajaCerrarInput, ArqueoCaja } from '../types'

export interface AperturaCajaCerrarVariables {
  id: string
  input: AperturaCajaCerrarInput
}

export const APERTURACAJACERRAR = gql`
  mutation AperturaCajaCerrar($id: ID!, $input: AperturaCajaCerrarInput!) {
    aperturaCajaCerrar(id: $id, input: $input) {
      ...ArqueoCajaFragment
    }
  }
  ${ARQUEOCAJAFRAGMENT}
`

export const useAperturaCajaCerrar = (
  options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaCerrarVariables>,
) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaCerrarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaCerrar: ArqueoCaja }>(APERTURACAJACERRAR, variables)
      return response.aperturaCajaCerrar
    },
    ...options,
  })
}
