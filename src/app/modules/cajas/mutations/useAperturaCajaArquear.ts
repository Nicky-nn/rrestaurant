// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query';
import { client, gql } from '../client';
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments';
import { AperturaCajaArquearInput, ArqueoCaja } from '../types';

export interface AperturaCajaArquearVariables {
  id: string;
  input: AperturaCajaArquearInput;
}

export const APERTURACAJAARQUEAR = gql`
  mutation AperturaCajaArquear($id: ID!, $input: AperturaCajaArquearInput!) {
    aperturaCajaArquear(id: $id, input: $input) { ...ArqueoCajaFragment }
  }
  ${ARQUEOCAJAFRAGMENT}
`;

export const useAperturaCajaArquear = (options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaArquearVariables>) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaArquearVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaArquear: ArqueoCaja }>(APERTURACAJAARQUEAR, variables);
      return response.aperturaCajaArquear;
    },
    ...options,
  });
};