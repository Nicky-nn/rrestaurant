// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query';
import { client, gql } from '../client';
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments';
import { ArqueoCaja, aperturaCajaAdicionarUsuarioInput } from '../types';

export interface AperturaCajaAdicionarUsuarioVariables {
  id: string;
  input: aperturaCajaAdicionarUsuarioInput;
}

export const APERTURACAJAADICIONARUSUARIO = gql`
  mutation AperturaCajaAdicionarUsuario($id: ID!, $input: aperturaCajaAdicionarUsuarioInput!) {
    aperturaCajaAdicionarUsuario(id: $id, input: $input) { ...ArqueoCajaFragment }
  }
  ${ARQUEOCAJAFRAGMENT}
`;

export const useAperturaCajaAdicionarUsuario = (options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaAdicionarUsuarioVariables>) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaAdicionarUsuarioVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaAdicionarUsuario: ArqueoCaja }>(APERTURACAJAADICIONARUSUARIO, variables);
      return response.aperturaCajaAdicionarUsuario;
    },
    ...options,
  });
};