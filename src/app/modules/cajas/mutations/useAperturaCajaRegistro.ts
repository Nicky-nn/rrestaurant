// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments'
import { AperturaCajaRegistroInput, ArqueoCaja, EntidadParamsInput } from '../types'

export interface AperturaCajaRegistroVariables {
  entidad?: EntidadParamsInput
  cajaId: string
  input: AperturaCajaRegistroInput
  modulo: string
}

export const APERTURACAJAREGISTRO = gql`
  mutation AperturaCajaRegistro(
    $entidad: EntidadParamsInput
    $cajaId: ID!
    $input: AperturaCajaRegistroInput!
    $modulo: String!
  ) {
    aperturaCajaRegistro(entidad: $entidad, cajaId: $cajaId, input: $input, modulo: $modulo) {
      ...ArqueoCajaFragment
    }
  }
  ${ARQUEOCAJAFRAGMENT}
`

export const useAperturaCajaRegistro = (
  options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaRegistroVariables>,
) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaRegistroVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaRegistro: ArqueoCaja }>(
        APERTURACAJAREGISTRO,
        variables,
      )
      return response.aperturaCajaRegistro
    },
    ...options,
  })
}
