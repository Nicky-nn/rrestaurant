// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query';
import { client, gql } from '../client';
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments';
import { AperturaCajaRegistroInput, ArqueoCaja } from '../types';

export interface AperturaCajaActualizarVariables {
  id: string;
  input: AperturaCajaRegistroInput;
  modulo: string;
}

export const APERTURACAJAACTUALIZAR = gql`
  mutation AperturaCajaActualizar($id: ID!, $input: AperturaCajaRegistroInput!, $modulo: String!) {
    aperturaCajaActualizar(id: $id, input: $input, modulo: $modulo) { ...ArqueoCajaFragment }
  }
  ${ARQUEOCAJAFRAGMENT}
`;

export const useAperturaCajaActualizar = (options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaActualizarVariables>) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaActualizarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaActualizar: ArqueoCaja }>(APERTURACAJAACTUALIZAR, variables);
      return response.aperturaCajaActualizar;
    },
    ...options,
  });
};