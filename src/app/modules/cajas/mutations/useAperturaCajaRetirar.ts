// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments'
import { AperturaCajaRetirarInput, ArqueoCaja } from '../types'

export interface AperturaCajaRetirarVariables {
  id: string
  input: AperturaCajaRetirarInput
}

export const APERTURACAJARETIRAR = gql`
  mutation AperturaCajaRetirar($id: ID!, $input: AperturaCajaRetirarInput!) {
    aperturaCajaRetirar(id: $id, input: $input) {
      ...ArqueoCajaFragment
    }
  }
  ${ARQUEOCAJAFRAGMENT}
`

export const useAperturaCajaRetirar = (
  options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaRetirarVariables>,
) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaRetirarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaRetirar: ArqueoCaja }>(
        APERTURACAJARETIRAR,
        variables,
      )
      return response.aperturaCajaRetirar
    },
    ...options,
  })
}
