// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query';
import { client, gql } from '../client';
import { ARQUEOCAJAFRAGMENT } from '../fragments/ArqueoCajaFragments';
import { AperturaCajaIngresarInput, ArqueoCaja } from '../types';

export interface AperturaCajaIngresarVariables {
  id: string;
  input: AperturaCajaIngresarInput;
}

export const APERTURACAJAINGRESAR = gql`
  mutation AperturaCajaIngresar($id: ID!, $input: AperturaCajaIngresarInput!) {
    aperturaCajaIngresar(id: $id, input: $input) { ...ArqueoCajaFragment }
  }
  ${ARQUEOCAJAFRAGMENT}
`;

export const useAperturaCajaIngresar = (options?: UseMutationOptions<ArqueoCaja, Error, AperturaCajaIngresarVariables>) => {
  return useMutation<ArqueoCaja, Error, AperturaCajaIngresarVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ aperturaCajaIngresar: ArqueoCaja }>(APERTURACAJAINGRESAR, variables);
      return response.aperturaCajaIngresar;
    },
    ...options,
  });
};