// AUTO-GENERATED BY ISI-GQL developer for Nick Russell

import { useMutation, UseMutationOptions } from '@tanstack/react-query'

import { client, gql } from '../client'
import { ARTICULOPRECIOGENERALFRAGMENT } from '../fragments/ArticuloPrecioGeneralFragments'
import { ArticuloPrecioGeneral, ArticuloPrecioImpresoraInput } from '../types'

export interface ArticuloPrecioImpresoraUpsertVariables {
  id: string
  input?: ArticuloPrecioImpresoraInput
}

export const ARTICULOPRECIOIMPRESORAUPSERT = gql`
  mutation ArticuloPrecioImpresoraUpsert($id: ID!, $input: ArticuloPrecioImpresoraInput) {
    articuloPrecioImpresoraUpsert(id: $id, input: $input) {
      ...ArticuloPrecioGeneralFragment
    }
  }
  ${ARTICULOPRECIOGENERALFRAGMENT}
`

export const useArticuloPrecioImpresoraUpsert = (
  options?: UseMutationOptions<ArticuloPrecioGeneral, Error, ArticuloPrecioImpresoraUpsertVariables>,
) => {
  return useMutation<ArticuloPrecioGeneral, Error, ArticuloPrecioImpresoraUpsertVariables>({
    mutationFn: async (variables) => {
      const response = await client.request<{ articuloPrecioImpresoraUpsert: ArticuloPrecioGeneral }>(
        ARTICULOPRECIOIMPRESORAUPSERT,
        variables,
      )
      return response.articuloPrecioImpresoraUpsert
    },
    ...options,
  })
}
